gcp
tarkistetaan että toimiiko gcp kuten pitää. se vitun funktio on maksanut mulle rahaa mutta se on toiminut kuten pitääkin. katsotaan huomenna mikä tilanne


lisätään pipelineen uusien docker imagejen buildaaminen ja pushaaminen gcp:lle

You can reduce noise by fixing your budget alert config, 
ensuring your Cloud Run returns success so Pub/Sub doesn’t retry, 
adding subscription filters (if attributes are present), 
setting a retry/dead-letter policy, or 
switching to pull to control flow.
